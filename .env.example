# =============================================================================
# ENVIRONMENT VARIABLES - Next.js + Supabase Starterkit
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# SITE CONFIGURATION
# -----------------------------------------------------------------------------

# The base URL of your application
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Your organization/site name (used in header, footer, meta tags)
# This will be displayed throughout the site
NEXT_PUBLIC_SITE_NAME="Your Organization Name"


# -----------------------------------------------------------------------------
# WHATSAPP CONFIGURATION
# -----------------------------------------------------------------------------

# WhatsApp number for contact button (format: country code + number, no spaces/dashes)
# Example: Indonesia +62 812-3456-7890 → 6281234567890
# Used in WhatsApp floating button and contact links
NEXT_PUBLIC_WHATSAPP_NUMBER=628123456789


# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------

# Supabase Project URL
# Where to find: Supabase Dashboard → Project Settings → API → Project URL
# Format: https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co

# Supabase Anonymous Key (Public Key)
# Where to find: Supabase Dashboard → Project Settings → API → Project API keys → anon public
# This is safe to expose in browser (protected by RLS policies)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (Secret Key)
# Where to find: Supabase Dashboard → Project Settings → API → Project API keys → service_role secret
# ⚠️  CRITICAL: NEVER expose this in client-side code! Only use in API routes.
# ⚠️  This key BYPASSES all Row Level Security (RLS) policies!
# Used for: Admin operations, user management, bypassing RLS when needed
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here


# -----------------------------------------------------------------------------
# FEATURE FLAGS - DATABASE MIGRATION CONTROL
# -----------------------------------------------------------------------------
# These flags control whether to fetch data from Supabase or use mock data
# Start with all FALSE during initial setup, then enable one by one after:
# 1. Running the database migration (supabase/migrations/20240122000000_initial_schema.sql)
# 2. Verifying the tables exist in Supabase Dashboard → Table Editor
# 3. Testing data access with your authenticated user
#
# Enable order (recommended):
# 1. NEXT_PUBLIC_USE_SUPABASE_MEMBERS=true (simplest table)
# 2. NEXT_PUBLIC_USE_SUPABASE_LEADERSHIP=true
# 3. NEXT_PUBLIC_USE_SUPABASE_ARTICLES=true (has RLS policies)
# 4. NEXT_PUBLIC_USE_SUPABASE_EVENTS=true (has RLS policies)
# 5. NEXT_PUBLIC_USE_SUPABASE_SETTINGS=true (admin only)
# -----------------------------------------------------------------------------

# Enable/disable Supabase for Members data (profiles)
# false = uses mock data from src/features/members/data/mock.ts
# true = fetches from 'members' table in Supabase
NEXT_PUBLIC_USE_SUPABASE_MEMBERS=false

# Enable/disable Supabase for Articles data
# false = uses mock data from src/features/articles/data/mock.ts
# true = fetches from 'articles' table in Supabase (RLS policies apply)
NEXT_PUBLIC_USE_SUPABASE_ARTICLES=false

# Enable/disable Supabase for Events data
# false = uses mock data from src/features/events/data/mock.ts
# true = fetches from 'events' table in Supabase (RLS policies apply)
NEXT_PUBLIC_USE_SUPABASE_EVENTS=false

# Enable/disable Supabase for Leadership data
# false = uses mock data from src/features/leadership/data/mock.ts
# true = fetches from 'leadership' table in Supabase
NEXT_PUBLIC_USE_SUPABASE_LEADERSHIP=false

# Enable/disable Supabase for Site Settings data
# false = uses default config from config/site.config.ts
# true = fetches from 'site_settings' table in Supabase (admin only)
NEXT_PUBLIC_USE_SUPABASE_SETTINGS=false


# =============================================================================
# SETUP CHECKLIST
# =============================================================================
# ✅ 1. Copy this file to .env.local
# ✅ 2. Create a Supabase project at https://supabase.com
# ✅ 3. Copy Project URL and API keys from Supabase Dashboard
# ✅ 4. Fill in NEXT_PUBLIC_SUPABASE_URL and keys above
# ✅ 5. Run the migration: Copy supabase/migrations/*.sql → Supabase SQL Editor → Run
# ✅ 6. Create your first admin: See README.md "Create First Admin" section
# ✅ 7. Enable feature flags one by one (start with MEMBERS=true)
# ✅ 8. Test each feature before enabling the next
# ✅ 9. Customize NEXT_PUBLIC_SITE_NAME and NEXT_PUBLIC_WHATSAPP_NUMBER
# ✅ 10. Deploy to production (don't forget to set env vars in Vercel/hosting)
# =============================================================================
